# Setup guidance

Use a local Android SDK compatible with the configured compile SDK. This starter should build with:

```bash
./gradlew :app:assembleDebug --no-daemon
```

Do not commit `local.properties` or other machine-local configuration.
