# Testing guidance

## Current commands

```bash
./gradlew :app:assembleDebug --no-daemon
```

Add focused unit or instrumentation tests when real features are introduced. Keep buildability checks separate from product validation evidence.
